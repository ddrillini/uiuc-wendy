mirror of the `util/` on the machine, which contains useful scripts. should
be periodically manually updated (or we could clone this repo to the
machine, to `~/uiuc-wendy` and then symlink `cd ~ ; ln -s uiuc-wendy/util
.`.
